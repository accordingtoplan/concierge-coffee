# Handover, 11 September 2026: launch round

State of the project as of today, the decisions taken with Benjamin this week, and the work list for this round. Read `CLAUDE.md` first. Notion `Website Build` page has the full log; this is the working brief.

**Updated the same evening.** Items 1, 2, 4, 5 and 6 are done and on the preview; item 3 is done except three admin writes; 7 and 8 are untouched. Each item below carries its outcome. Commits on `main`, in order: 676045c, df29604, bdbe3ea, 11ca8df, 6114ee8, a635ab3, 742fefa, 7bea41e, 8e15369, b3da8b1, d7cf667. Branch `cloudflare` is on the remote, two commits ahead, unmerged.

**Decided during the day, Frederik with Benjamin's email:**

- Benjamin's copy is in on About (intro, Two cities, The coffee, Abrazo) and Arrangements (intro, Events, Catering, Wholesale). His words, with grammar fixes and one clause reshaped.
- Drinks: no menu, no list, no shop. What remains is four photographs on the homepage in a two-column square grid, name, ingredients and price from the store, nothing to click. Benjamin's four: Einspänner, Yuzu Matcha Tonic, Latte iced, Saffron Latte (the stoop photo, carried in the repo as the Saffron fallback; the product is still Draft in Shopify and a stub in `shopify.js` fills it until it goes Active). The seven other drink photographs are removed from Shopify. The credits line is gone. `DRINKS_ORDERABLE` in `shopify.js` still guards the product page and the quick add panel; the pickup slot code stays; the old menu and shop markup is in history at 11ca8df.
- Merch is off the site: sections, footer link, list and styles. The four images left the repo with the other unreferenced files.
- Homepage "The evening" fact is out (licence not official); the Abrazo section stays with Night markets and the slider. About and Arrangements keep their Abrazo copy.
- The mid-century barista photograph is out; Benjamin at the Berlin machine holds the homepage About slot until his own barista shots arrive.
- Wholesale block on Arrangements carries Benjamin's four photographs from 11 Sep (warehouse, sacks in the car, beans in the roaster, plant in light) two up under the form.
- Shopify prices differ from the sheet in the project CLAUDE.md: Colombia 25.50 and 85, Peru 23 and 65, Espresso 1kg 58, Decaf 1kg 59. Not touched; confirm with Benjamin.

## Where things stand

The site is built, hardened and verified on the preview domain. Last commits: shared `styles.css` replacing six inline copies, Shopify failure states, images to WebP, meta and OG tags, sitemap, robots, 404, keyboard access for the card grids and dialogs, an Abrazo section on the homepage and on Arrangements. Every remaining launch item is either hosting, Shopify data, or a decision from the client. None of it is site code, except the list below.

**Sales channel decisions, Benjamin, 11 Sep:**

- Shop sales stay in Square, bags included. Square has a beans category and they read category performance there. No Shopify POS, no sync app.
- Shopify is the online channel only: web shop, wholesale invoicing, and whatever pickup ordering gets agreed.
- **Pickup orders, drinks and bags collected at the shop, run through Square Online, not the site.** Decided by Benjamin, second reply 11 Sep: orders arrive in the Square POS, get made, the customer collects; same as a drink ordered for pickup today. Square Online therefore stays, moves to `order.conciergecoffee.com`, and the site links to it. Compare notes a few weeks after it is live. The drinks pickup built into the site on 1 and 2 Sep is switched off, see item 2, and the crew order page (item 6) is not built.
- Wholesale: requests arrive at `orders@conciergecoffee.com`, processed daily, invoiced from Shopify (draft order, invoice by email, card payment). On the site: a request form. Shopify B2B is Plus only; not an option. The bookkeeper only needs a monthly report; Benjamin is asking him what is easiest.
- Shipping: whole US from day one, same-day shipping, flat rate preferred. Benjamin bags, five days a week, and covers the other two. He is buying mailer boxes, which set the rates. Frederik owns the Shopify side, Benjamin the physical side; see item 7.
- Launch scope: three coffees on sale, Colombia filter 300g, Peru filter 300g, Espresso 300g, plus 3 to 5 of each 1kg. Everything else sold out or "not online yet". Merch stays "not online yet" with a few placeholders; details to discuss. Hours as on Google Maps, confirmed. Copy and image feedback from Benjamin has arrived by email; Frederik holds it.
- Email on the .com is live on Google Workspace since 11 Sep: personal addresses for Benjamin and Namy, `orders@` created, `hello@` is an alias placeholder. `wholesale@conciergecoffee.de` is retired for the US site.
- The .de domain is parked, US first. Registered at Hostnet, Marick has access, site on Squarespace, mail on Google.

## Work list for this round

In order. Items 1 to 6 can be done now. Item 7 needs box weights from Benjamin. Item 8 is Frederik's, outside the repo.

### 1. Email addresses on the site

**Done.** `orders@conciergecoffee.com` on Arrangements. The Berlin block on Visit still links conciergecoffee.de on purpose; it is a placeholder site that gets replaced.

Replace `wholesale@conciergecoffee.de` (two occurrences: `arrangements.html` line 163 and `visit.html`) with `orders@conciergecoffee.com`. Grep for any other `.de` address or reference to the German site before closing this. `mailto:` links only; no forms yet.

### 2. Drinks: menu visible, buying off

**Done, then overtaken.** The switch went in as described, the menu rendered as a photo row plus a list, and the same day the list, the shop page drinks and the footer link came out again (see the decisions above). What stands: four photographs, two columns, nothing to buy. `ORDER_URL` moved to `layout.js` so content pages do not load the storefront; it is still empty.

The drinks section on `index.html` renders from Shopify (`renderDrinks()`, products tagged `drink`, eleven of them active at quantity 0) with a pickup time slot and bar-hours gating in `shopify.js`. Decided 11 Sep: pickup ordering lives in Square Online, not on the site. The drinks read as a menu, not a shop, and the menu points at the Square ordering page.

Do this with **one switch in `shopify.js`**, for instance `const DRINKS_ORDERABLE = false;`, so it can be flipped back without touching markup. When false: drink cards render name, ingredients and price with no buy control, no pickup field, no "sold out" state; the "Order online, pick up at the bar" line on `index.html` becomes a line with a link: "Order ahead for pick-up" to `https://order.conciergecoffee.com` (leave the href behind a constant `ORDER_URL` in `shopify.js`; the subdomain does not exist until item 8 creates it, so until then the line reads "Drinks at the bar, 821 Traction Ave" with no link); the product page refuses drink handles (redirect to `index.html#menu`); the quick add panel never opens for a drink. Do not delete the pickup code; the decision is "compare notes after a few weeks", so it may come back. Leave the drink products in Shopify as they are.

Check the fallback path too: if Shopify is down, the drinks section should still show the menu or hide itself cleanly, as it does today.

### 3. Shopify inventory, so ten bags means ten bags

**Mostly done.** Set: Peru 300g 10, Peru 1kg 3, Espresso 300g 15, Espresso 1kg 3, Decaf 0; policy Deny on Colombia, Peru, Decaf and the draft TBD. The permission layer blocked three writes; still to do in the admin: Colombia 300g to 10, Colombia 1kg to 3, Espresso 300g and 1kg policy to Deny. Verified on the preview: Espresso and Peru buyable in both sizes, Decaf sold out, a two-bag add came back from Shopify's cart at $46.00.

In the Shopify admin (Frederik has access; the Cowork session has the Shopify connector). Every coffee variant:

- Inventory policy **deny** when out of stock. Several were left on **continue** for testing and would let the site oversell.
- Quantities: Colombia 300g **10**, Peru 300g **10**, Espresso 300g **15**, Colombia, Peru and Espresso 1kg **3** each (Benjamin said 3 to 5), Decaf Colombia **0**, `TBD` (`espresso-roast`) stays Draft. Colombia and Espresso 300g currently sit at **250** from testing; that is the number to correct.
- Benjamin tops the number up when he roasts. That is the whole routine; no code.

Then verify on the preview: the three coffees buyable in both sizes, Decaf reads sold out, checkout reaches Shopify with the right quantity in the cart mutation body.

### 4. Wholesale page with a request form

**Done, Function route.** Form under Benjamin's wholesale copy, posting to `functions/api/wholesale.js` (Cloudflare Pages Function, Resend, honeypot, validation, JSON for the script and a redirect for a plain post). On GitHub Pages there is no function, so the page hands the request to the visitor's mail app, prefilled. Needs at Cloudflare time: a Resend account with conciergecoffee.com verified, `RESEND_API_KEY` on the Pages project, and a rate-limit rule on `/api/wholesale` so a script cannot burn the Resend quota. Minimum order still not in the copy.

New section or page under Arrangements (the `#wholesale` block on `arrangements.html` already exists with copy and two photographs; extend it rather than adding a page unless it gets long). A short form: business name, contact name, email, phone, which coffees and roughly how many kilos a month, delivery or pickup, a free text line. It mails `orders@conciergecoffee.com`.

Static HTML cannot send mail. Options, pick one and note it in the commit:

- **Cloudflare Pages Function + Resend** (free tier, one API key as a Pages environment variable, domain verified by DNS once the zone is on Cloudflare). Cleanest once hosting has moved; a `functions/api/wholesale.js` handler, honeypot field, server-side validation, redirect to a thank-you state on the same page. Cannot be tested on GitHub Pages; test with `wrangler pages dev`.
- **A form service** (Web3Forms or Formspree free tier) if the form has to work before Cloudflare is live. Swap to the Function later.

Wholesale prices do not go on the page. Minimum order: not answered; leave the copy open. The page copy follows the voice rules: what wholesale is, who it is for, how it works, the form. No "partner with us".

### 5. Cutover branch

**Done, pushed, unmerged.** Branch `cloudflare`: the 22 URLs, CNAME gone, `_headers`, `_redirects`. Two deviations: the stylesheet and modules cache one hour rather than a year because nothing versions their names, and canonical, OG and sitemap URLs are the clean form (`/shop`), because Cloudflare Pages serves `shop.html` there and redirects the long form. Internal links keep `.html`. The Content-Security-Policy was tested against the real homepage.

Branch `cloudflare`, to merge the day DNS moves:

- Every `https://concierge-coffee.accordingtoplan.co` becomes `https://conciergecoffee.com` (22 occurrences across HTML canonical and OG tags, `sitemap.xml`, `robots.txt`).
- Delete `CNAME` (GitHub Pages only).
- Add `_headers` for Cloudflare Pages: long cache on `/images/*`, `/fonts/*`, `styles.css`; `X-Content-Type-Options: nosniff`; a permissive `Content-Security-Policy` that still allows `concierge-coffee-2245.myshopify.com` and `cdn.shopify.com`.
- Add `_redirects`: `https://www.conciergecoffee.com/* https://conciergecoffee.com/:splat 301` (or the reverse, pick one canonical host and keep it).
- Do not merge to `main` until told. `main` still serves the preview.

### 6. Pickup orders: link to Square Online

**Done as far as it goes.** The footer link and the homepage line appear once `ORDER_URL` in `layout.js` is set. The crew line on Arrangements is not written yet; it waits for the subdomain.

Decided: **A**. Nothing to build in this repo beyond the links. `ORDER_URL` from item 2 also goes into the footer Shop column ("Order for pick-up") and, once the Arrangements copy is revised, one line in the events and catering block for crews: order ahead at the same address, or call. No `crew.html`, no order form on the site. Square Online configuration, menu and the custom subdomain on the Square side are Benjamin's; the DNS record for `order.conciergecoffee.com` is Frederik's, item 8.

### 7. Shipping: whole US, same day, flat rate

Set in the Shopify admin, not in the repo. Decided: one shipping zone, United States, flat rate, orders placed before a cutoff ship the same day. Benjamin bags and ships; he is buying mailer boxes.

How the flat rate gets its number: Shopify Basic includes Shopify Shipping (discounted USPS and UPS labels bought inside the order screen, no carrier account needed). Once Benjamin has a box, weigh one 300g bag packed and one 1kg bag packed, enter the weights on the variants, and read the USPS Ground Advantage label price from Shopify Shipping for a Los Angeles to New York address and a Los Angeles to San Francisco address. The flat rate is roughly the average of the two for a single 300g bag; round to a clean number and consider free shipping above a threshold that covers two or three bags. Set the same-day cutoff in the shipping policy copy on the product page and in the order confirmation email (something like "ordered before 12 pm Pacific, ships the same day"); do not promise it in the hero.

Also enable **local pickup** at 821 Traction as a Shopify pickup rate for bags. It costs nothing and it is the free option for LA customers.

Before launch: place a test order to a friend's address in another state and confirm the shipping step shows the flat rate, the label can be bought, and the confirmation email reads right.

### 8. Hosting and DNS, Frederik, outside the repo

**Not started.** Split agreed: the account, the sign-ins and the nameserver change are Frederik's; once he is signed in to Cloudflare in his own Chrome, the zone check, the Pages project, the `pages.dev` sign-off, the custom domains, the merge and the checkout test can be driven from there step by step.

Sequence lives on the Notion page. Short form: Cloudflare account under `hello@conciergecoffee.com` (Benjamin forwards the verification mail); add `conciergecoffee.com` as a zone; confirm the MX (`smtp.google.com`) and both `google-site-verification` TXT records came across; change nameservers at Namecheap (Frederik has the login); Workers and Pages project from this repo, production branch `main`, no build command, output `/`; sign-off on the `pages.dev` URL; add the custom domains, Cloudflare writes the DNS itself; merge `cloudflare` to `main`; test checkout on the real domain. Rollback is the old A record `199.34.228.186` (Square Online, a placeholder page on a paid tier). Square Online stays: it becomes the pickup ordering page at `order.conciergecoffee.com`. In Square Online, Benjamin connects that subdomain as the site's custom domain; Cloudflare gets the CNAME record Square specifies. Nothing is cancelled. The .com registration transfers from Namecheap to Cloudflare Registrar after launch, separately; needs a card on the Cloudflare account, Benjamin adds it himself.

## Still open with the client, not blocking

- Photography: pack shots from the label files, the founding story for About, Benjamin's old barista shots for the homepage About slot. The 11 Sep video in the project images folder (320 by 568, eleven seconds) has no place on the site yet.
- Whether the homepage Abrazo section should go entirely, given the licence.
- Bookkeeper: needs a monthly report only. Simplest is a Shopify staff account with reports-only permission, or a monthly sales export emailed from Shopify Reports. Benjamin is asking him; no QuickBooks sync until he asks for one.
- Merch: off the site as of 11 Sep. Comes back from history (11ca8df) when there is something to sell.
- Production costs in the price sheet are carried over from old origins; `DECOL` breaks the three-letter SKU convention; the `TBD` filter has no name. Benjamin's.
- `prototype.html` is gone from the tree; nothing to do.

## How to work with Frederik on this

State the recommendation, skip caveats that do not change the decision. Two or three options with a pick, never a menu. Push back once if something is not sharp, then follow his call. Fix the pattern, not the instance. Write into his working tree; he commits and pushes. List file names and what changed at the end of a session; he logs it to Notion.

## Also done 11 Sep, outside the list

- Phone pass on every page: the `hidden` attribute now beats display rules (it had left the pick-up time field showing on coffee), tap targets reach 44px under a thumb, labels read at 11px on a phone, every local photograph has an 800px variant with `srcset`, Shopify photographs get 400, 800 and 1200 candidates. Homepage on a phone went from 1.4MB to about 770KB.
- Bag: a failed call leaves the bag as it was; a stored id that outlived its cart starts a fresh one; a restore during an outage keeps the ticket; Shopify's refusals show on the button instead of closing the panel; every line has Remove; a drink line from the old test bags is purged on restore.
- The phone nav sheet closes when a link is tapped (it never did; the listener moved into `layout.js`). The current page is marked on clean URLs too.
- Dead code out: `allProducts`, `renderFooterLinks`, `CONCEPTS`, an unused crop hook, orphaned style rules, 47 unreferenced images.
- Repo now carries `CLAUDE.md` and this handover. A local preview config sits at the project root, outside the repo, as `.claude/launch.json`.
